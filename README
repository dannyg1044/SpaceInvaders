Space invaders game made with python.
This little project was made for the purpose of getting used to coding in python as a former java user.
The program will only run if pygame is installed as it is imported and is the main set of modules used to create the game.
